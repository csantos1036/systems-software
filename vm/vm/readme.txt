Author Names: Carolina Santos and Jacob Campbell 

To compile and run the VM, the input file name will be read as a command line argument at runtime, such as $ ./a.out test1.txt. The program will be printed to the screen. Every instruction will be printed line by line. 